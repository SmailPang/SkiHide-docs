# 使用说明

## 窗口隐藏 / 恢复逻辑

SkiHide 的操作逻辑是：

- 选中一个窗口 → 按下快捷键 → **隐藏窗口**
- 再次按下同一快捷键 → **恢复窗口**

隐藏的窗口不会关闭程序，仅从桌面消失。

## 窗口列表说明

列表中只显示：

- 可见的普通窗口
- 已过滤系统窗口 / 工具窗口
- 不包含 SkiHide 自身窗口

## 隐藏效果说明

- 窗口不会最小化到任务栏
- 程序仍在后台运行
- 再次恢复时保持原有状态
